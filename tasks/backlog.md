# Задачи

`A` — в работе · `W` — ждёт ответа · `B` — отложено · `D` — сделано

Новая задача — сверху. Меняется только буква в начале строки.

- `B` **Обновления в Android-сборке** — отложено до маркета.
  В APK всё лежит внутри, `version.json` приложение спрашивает у себя же, поэтому новых
  версий не видит. Нужны `VITE_VERSION_URL` на сайт и `VITE_STORE_URL`: кнопка обновления
  ведёт в Google Play, блокировка устаревших версий работает как в вебе. ~20 минут.
